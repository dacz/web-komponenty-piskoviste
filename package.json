{
  "name": "webcomponents",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "min-dns": "terser --compress --mangle --output dns-record/dnsRecord.min.js -- dns-record/dnsRecord.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "clean-css": "^5.3.2",
    "html-minifier-terser": "^7.2.0",
    "terser": "^5.24.0"
  }
}
